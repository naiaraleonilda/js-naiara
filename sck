function setup() {
  createCanvas(400, 400);
  background("pink");
}

function draw() {
  rect(30, 20, 20);
  fill("violet");
  stroke("black");
  if (mouseIsPressed);
  {
    rect(mouseX, mouseY, 20, 20);
  }
}
